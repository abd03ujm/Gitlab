type: pipeline   # Specify the type as 'pipeline' for a pipeline execution policy
name: "Block MR if vulnerabilities found"
enabled: true
rules:
  - type: pipeline # The policy applies when a pipeline runs
    cadence: all    # Apply on every pipeline run
    actions:
      - type: block_merge_request  # Block merge requests if vulnerabilities are detected
